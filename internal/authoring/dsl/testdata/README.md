testdata/

This directory contains golden files used by parser regression tests.

The JSON files represent canonical AST outputs for specific DSL
expressions. Tests compare the current parser output against these
files to detect unintended changes in parsing behavior.